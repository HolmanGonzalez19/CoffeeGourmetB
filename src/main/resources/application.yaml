spring:
  application:
    name: CoffeeGourmetB

  datasource:
    url: jdbc:postgresql://localhost:5432/coffeegourmet
    username: postgres
    password: TU_PASSWORD
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080